// Fill out your copyright notice in the Description page of Project Settings.


#include "UI/Widget/PackageFrameWidget.h"

void UPackageFrameWidget::InitPanel(int32 ItemID)
{
	if (ItemID == -1)
	{
		// FrameName->SetVisibility(ESlateVisibility::Hidden);
		// Icon->SetVisibility(ESlateVisibility::Visible);
		// if (FItemBase* ItemBase = GetWorld()->GetGameInstance()->GetSubsystem<UItemSubsystem>()->GetItemData(ItemID))
		// {
		// 	Icon->SetBrushFromTexture(ItemBase->Icon);
		// }
	}
}
